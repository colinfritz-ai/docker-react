language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t konungr/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true konnungr/docker-react npm run test

